# Cloudflare Pages Deployment Guide

## ⚡ Quick Setup

### Step 1: Deploy PartyKit Backend

```bash
npm run party:deploy
```

This will prompt you to login with GitHub and deploy. You'll get a URL like:
`https://open-catan.YOUR-GITHUB-USERNAME.partykit.dev`

**Copy this URL!**

### Step 2: Setup Cloudflare Pages

1. Go to [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Click **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
3. Select your **OpenCatan** repository
4. Configure build settings:

| Setting | Value |
|---------|-------|
| Production branch | `master` |
| Build command | `npm run build` |
| Build output directory | `dist` |

5. Add **Environment Variables**:
   - `NODE_VERSION` = `20`
   - `VITE_PARTYKIT_HOST` = `open-catan.YOUR-USERNAME.partykit.dev` (from Step 1)

6. Click **Save and Deploy**

### Step 3: Done! 🎉

Your site will be live at: `https://YOUR-PROJECT.pages.dev`

---

## 🔄 How Deployments Work

- **Frontend**: Auto-deploys on every push to `master` via Cloudflare Pages
- **Backend**: Run `npm run party:deploy` when you change backend code (party/ folder)

## 📱 Your URLs

After setup, you'll have:
- **Frontend**: `https://opencatan.pages.dev` (from Cloudflare)
- **Backend**: `https://open-catan.your-username.partykit.dev`

## 🛠️ Manual Deploy Command

Deploy everything at once:
```bash
npm run deploy
```

## 🌐 Custom Domain (Later)

Add your custom domain in:
**Cloudflare Pages** → **Your Project** → **Custom domains**
